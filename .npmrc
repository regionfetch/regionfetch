# Provenance is attached automatically by npm trusted publishing (OIDC) when the
# release workflow runs. It is deliberately NOT set here: a repo-wide
# `provenance=true` makes every local `npm publish` fail, because provenance can
# only be generated from a supported CI environment.
